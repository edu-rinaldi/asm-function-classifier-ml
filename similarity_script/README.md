# Similarity script for comparing blind tests results
Given 2 .txt files with model results on blindtest it gives hamming distance and cosine similarity between the two files.

# How to use it
Firt check requirements:

```bash
pip install -r requirements.txt
```

then just run:

```bash
python3 similarity.py fpath1 fpath2
```
